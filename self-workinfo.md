# 算法、软件、软硬结合方向的方案设计

软硬结合是指通过软件控制硬件，基于硬件的控制结果反馈软件，作为数据分析的基础；比如飞机高度控制、化工流程工艺控制、基于无人机视觉的高度参数控制图像清晰度；

## 1、工作内容

1）对已有项目（即客户的项目已经完成）进行专利保护；  
2）未来的项目（还未进行或处于规划阶段），提供专利方向的布局；

## 对于情况1

通常设计的具体方案严重依赖于客户所提供的材料，对于其中可以挖掘的创新点（比如相关技术问题的解决）进行算法的优化，优化的具体方式依赖于所提供的材料（材料中通常包含企业的说明、项目计划书、极少部分数据（工程数据、图纸、图像等）），并且受限于项目的完成情况，通常此类专利不能大幅度更改，因此具体的模型选择基于客户的选择，影响对于方案的选择（通常会设计多个方案，由客户选择其中一个进行后续的完善和更改）；只对其中一个方向（一个专利只能申请一个创新点，一个项目可以申请多个专利）能够进行专利三性（专利需要满足：创新性、新颖性、实用性）分析。因此对于专利非创新部分通常采用现有方案进行表述进行简要说明。

### 以下是示例

阵列针脚检测（已说明）；  
钢板边浪的缺陷检测（边缘部分呈现波浪形弯曲）



**客户资料：**  
少量图片，原理图，工作图，说明图；

**技术背景：**  
由于受到反光影响，导致对于缺陷的检测错误率严重偏高；

**现有问题：**  
光照变换消除光照影响，比如HSL、或者计算每个像素的光照强度，通过原图像-光照强度实减小关照强度的影响；但是由于边浪缺陷属于同一材质，形变微小，且边浪本身就通过反光现象进行识别，导致消除光照影响后对于边浪的识别没有有效改进，（不同于划痕裂缝、气泡等缺陷，消除光照后能够进行有效识别）。

**具体分析措施：**  
对于具体的缺陷类型，通常在特征构建的过程中基于具体缺陷进行分析：  
边浪位置（边缘）、形状（凸性、对称性）等构建特征，目标检测获得钢板，得到钢板的长边；通过光照变换获得图像中每个像素点的光照影响；计算像素点的光照方向（梯度），基于凸性特征，梯度从边缘想着钢板内部具有明显的方向特征（凸性），计算相邻两列的所分析的像素点梯度方向是否为连续变化，钢板边缘像素点的特征筛选，直到遍历完整个图像的相邻列或者不再满足连续性；得到像素点序列。（由于可能存在多个符合要求的像素点梯度，可能会一个初始像素点存在多个遍历序列）；得到遍历二值图；对每序列分析进行形状（类二次型，拟合）和对称性（梯度方向的对称性，基于二次型的对称轴）；对二值图进行序列的筛选；输出判断结果以及缺陷位置；

**有益效果：**  
不改变现有的钢板缺陷识别网络结构，因此上述述边浪特征的分析可以直接集成到现有的网络提取的特征中，实现对于所有缺陷的识别，并且所增加的特征并不影响对于其余缺陷的识别，并提高对于钢板缺陷识别的准确性；

## 对于情况2

通常允许为对架构进行设计，可以对模型调整或者设计符合需求的模型，可以调节的尺度较大。同时具体的细节也需要进行设计；该情况下客户也没有细节和架构进行分析和把握，只有明确的任务目标。在设计过程中可以将所设计的架构与其余架构进行关联性处理。

### 示例说明（1）

无人机林业的虫害识别（对虫害进行识别，无人机投放天敌）

**客户资料：**  
图像，虫害等图像；

**背景技术：**  
识别是需要获得具体的虫害位置，对所在位置的虫害进行识别，判断是否需要进行投放；

**现有方案：**  
传统识别采用目标追踪以及昆虫颜色等进行识别，因此需要获得具有明显差异的特征点，作为分析对象，基于特征点获得所在领域，将其作为虫害区域，对该区域进行虫害识别；但是容易受到环境噪声的影响，如无人机飞行、环境噪声（风）等，以及昆虫的飞行和移动；导致存在大量不表示虫害的区域需要进行后续的识别判断；

**解决措施：**  
通过无人机光流法获得在二维图像上的运动方向；同时记录真实的飞行轨迹；需要测距；  
对图像中主要的内容进行保留，其余部分mask，（主要部分是指不受环境干扰的部分，比如树木枝干等）。该部分的图像标注可以采用以下方式进行处理：在飞行前处于相对静止状态，同样计算光流，获得运动的部分，将非运动的部分进行保留，并进行语义分割；得到mask图像；之后无人机处于运动状态，采用目标追踪得到运动时的mask图像，通过上述产生目标数据；之后计算运动过程分割部分的运动光流；（X，y）  
基于上述运动图像、无人机飞行信息、目标位置信息构建神经网络（可以基于空间特征以及方向投影进行建模），将上述数据作为输入，输出运动光流，完成模型的训练；将该网络作为飞行方向转换网络；作用是将三维的飞行方向转化为二维图像上的运动方向；记为f(X)；  
训练完成后，输入无人机的实时飞行图像，计算像素点光流，以及运动光流；进行向量分解，得到相对运动光流；对运动光流进行无监督异常检测（基于0），得到异常像素点以及正常光流的范围；光流范围需要根据运动光流进行确定；  
于是得到每帧的正常光流以及范围；  
将处于异常光流像素点进行目标追踪，若存在连续多次存在异常，则标注所追踪的目标存在异常；

**有益效果：**  
解决了运动物体以及环境因素带来的干扰，通过将三维运动结果反馈到二维图像，确定物体的相对运动光流，提高了对于虫害位置判断的准确性，同时缩减所需要检查的位置。另外，上述过程可以与RCNN算法相结合，比如基于光流位置实现对于分块的选择，将运动光流特征与模型输出进行结合。体现发明的单一性。
